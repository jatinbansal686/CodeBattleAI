// client/src/context/SocketContext.js
import React from 'react';

// The context will hold our single socket connection.
// We initialize it with null.
export const SocketContext = React.createContext(null);